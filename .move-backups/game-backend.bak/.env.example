# Copy this file to `.env` and fill in your database credentials. Do NOT commit real secrets.

# MySQL connection
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=change_me
DB_DATABASE=ww_db
DB_PORT=3306

# Optional
NODE_ENV=development
